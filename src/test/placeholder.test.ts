// Placeholder test — confirms the test runner is configured correctly.
// Replace or remove once real tests are added.

import { describe, it, expect } from 'vitest';

describe('scaffold', () => {
  it('test runner is configured correctly', () => {
    expect(true).toBe(true);
  });
});

// Contact form with name, email, and message fields.
// Submits to Formspree endpoint from src/data/contact.ts.
// Shows inline validation errors, success message on submit, and error message on failure.

export default function ContactForm() {
  return null;
}

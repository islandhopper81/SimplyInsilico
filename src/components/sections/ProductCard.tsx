// Displays a single product: image, name, and tagline.
// Clicking the card navigates to the product URL (external opens in new tab, internal uses Next.js Link).

export default function ProductCard() {
  return null;
}

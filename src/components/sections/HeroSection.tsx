// Landing page hero: headline, subheadline, and CTA buttons linking to /services and /products.
// Uses Framer Motion whileInView for scroll-triggered fade-in animation.

export default function HeroSection() {
  return null;
}
